'use client';

/**
 * DEPRECATED: App now uses Public RPCs via Ankr.
 * This component is kept as a placeholder to prevent import errors.
 */

export default function ApiKeyManager({ isOpen, onOpenChange }: { isOpen: boolean, onOpenChange: (open: boolean) => void }) {
  return null;
}
