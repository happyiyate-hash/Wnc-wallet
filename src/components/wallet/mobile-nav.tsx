'use client';

/**
 * DEPRECATED: Navigation is now handled exclusively via Sidebar or Header nodes.
 * Returning null to purge the bottom navigation from the terminal interface.
 */
export default function MobileNav() {
  return null;
}
